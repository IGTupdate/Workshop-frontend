export type TVehicle = {
    _id: string;
    vehicle_make: string;
    vehicle_model: string;
    vin: string;
    registeration_number: string;
    owner: string,
    createdAt: string,
    updatedAt: string
};
